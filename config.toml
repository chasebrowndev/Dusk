# Dusk config — shared between both machines.
# Role is set by the launcher script, not here.

name    = "Chase"
host_ip = "100.x.x.x"
port    = 7337
width   = 900
height  = 640

[env]
GDK_BACKEND                              = "x11"
WEBKIT_DISABLE_SANDBOX_THIS_IS_DANGEROUS = "1"
